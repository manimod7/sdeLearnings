package lld.elevatorsystem;

/**
 * Enum representing the direction of elevator movement
 * Used in the strategy pattern for elevator scheduling
 */
public enum Direction {
    UP,
    DOWN,
    IDLE
}